# Phase 2 — Cursor Paging Verification

Pages using `fetchCasesPage`:
- Admin Dashboard (cases list)
- Case Form (auto-title/module code lookup)

Checklist:
- [ ] Admin Dashboard: apply a filter, click Next/Prev, confirm items change and buttons disable at ends.
- [ ] Admin Dashboard: change sort or filters, confirm paging resets to page 1.
- [ ] Case Form: create new case, ensure auto-title/module code still resolve (no errors in console).

Results:
- Not run (manual verification required).
